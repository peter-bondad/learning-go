version: "2"

sql:
  - engine: "postgresql"
    queries: "infra/db/queries"
    schema: "infra/db/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
